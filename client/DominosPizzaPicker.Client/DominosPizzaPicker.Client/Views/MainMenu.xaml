<?xml version="1.0" encoding="UTF-8"?>
<c:CustomMainMenu xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:c="clr-namespace:DominosPizzaPicker.Client.Helpers.CustomViews"
             xmlns:local="clr-namespace:DominosPizzaPicker.Client.Views"
			 x:Class="DominosPizzaPicker.Client.Views.MainMenu"
			 Title=""
             BackgroundColor="#3F51B5">
    <ContentPage.Content>
        <Grid HorizontalOptions="CenterAndExpand" Padding="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" VerticalOptions="CenterAndExpand" Padding="60,0,60,50">
                <StackLayout Padding="0,0,0,50">
                    <Image Source="pizza_main_menu.png"/>
                </StackLayout>
                <Button x:Name="btnRandom" Text="Pick Random Pizza" Command="{Binding NavigateCommand}" CommandParameter="{x:Type local:PickRandom}" Font="Large" BorderWidth="1" Margin="15,5"/>
                <Button x:Name="btnUpdate" Text="Update Pizza" Command="{Binding NavigateCommand}" CommandParameter="{x:Type local:UpdateRecentList}" Font="Large" BorderWidth="1" Margin="15,5"/>
                <Button x:Name="btnStats" Text="Pizza Stats" Clicked="OnbtnStatsClicked" Font="Large" BorderWidth="1" Margin="15,5"/>
                <!--<Button x:Name="btnLocal" Text="Local URL" Command="{Binding ConnectCommand}" CommandParameter="local" Font="Large" BorderWidth="1" Margin="15,5" />
                <Button x:Name="btnConveyor" Text="Conveyor URL" Command="{Binding ConnectCommand}" CommandParameter="conveyor" Font="Large" BorderWidth="1" Margin="15,5" />
                <Button x:Name="btnLocal" Text="Local URL" Command="{Binding ConnectCommand}" CommandParameter="local" Font="Large" BorderWidth="1" Margin="0" />
                <Button x:Name="btnConveyor" Text="Conveyor URL" Command="{Binding ConnectCommand}" CommandParameter="conveyor" Font="Large" BorderWidth="1" Margin="0" />-->
            </StackLayout>
            <StackLayout Grid.Row="0" HorizontalOptions="CenterAndExpand" VerticalOptions="EndAndExpand" Padding="0,0,0,45">
                <StackLayout HorizontalOptions="CenterAndExpand" Padding="20,0,20,15">
                    <Label x:Name="lblURL" Text="" Margin="0" TextColor="Black" FontSize="Small"/>
                </StackLayout>
                <Grid ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <StackLayout Grid.Column="0" HorizontalOptions="EndAndExpand" VerticalOptions="CenterAndExpand">
                        <Button x:Name="btnLocal" Text="Local URL" Command="{Binding ConnectCommand}" CommandParameter="local" Font="Medium" BorderWidth="1" Margin="0" WidthRequest="125"/>
                    </StackLayout>
                    <StackLayout Grid.Column="1" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand">
                        <Button x:Name="btnConveyor" Text="Conveyor URL" Command="{Binding ConnectCommand}" CommandParameter="conveyor" Font="Medium" BorderWidth="1" Margin="0" WidthRequest="125"/>
                    </StackLayout>
                </Grid>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</c:CustomMainMenu>